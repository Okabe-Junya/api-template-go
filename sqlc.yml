version: "1"
packages:
  - name: "db"
    path: "internal/db"
    queries: "./sql/queries.sql"
    schema: "./sql/schema.sql"
    engine: "mysql"
    emit_json_tags: true
    emit_prepared_queries: false
    emit_interface: false
